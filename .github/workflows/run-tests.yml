name: 🧪 자동화 테스트 실행

on:
  workflow_dispatch:
    inputs:
      test_target:
        description: '📋 테스트 선택'
        required: true
        type: choice
        default: '🎯 전체 테스트'
        options:
          - '🎯 전체 테스트'
          - '👤 구성원 (User)'
          - '📇 외부 연락처 (Contact)'
          - '👥 그룹 (Group)'
          - '🌐 외부 그룹 (External Group)'
          - '🏢 조직 (OrgUnit)'
          - '💼 직책 (Position)'
          - '📊 직급 (Level)'
          - '📌 상태 (Status)'
          - '👔 사용자 유형 (UserType)'
          - '🏷️ 회사 태그 (Company Tag)'
          - '📍 MY 태그 (MY Tag)'
      
      browser:
        description: '🌐 브라우저'
        required: false
        type: choice
        default: 'chromium'
        options:
          - chromium
          - firefox
          - webkit

jobs:
  test:
    runs-on: ubuntu-latest
    
    # 테스트 이름을 동적으로 표시
    name: ${{ inputs.test_target }}
    
    container:
      image: mcr.microsoft.com/playwright/python:v1.45.1-jammy
    
    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4
      
      - name: 📦 의존성 설치
        run: |
          pip install --no-cache-dir -r requirements.txt
          playwright install
      
      - name: 🔧 환경 설정
        run: |
          cat << EOF > .env
          TEST_ENV=${{ secrets.SERVER }}
          REAL_DOMAIN=${{ secrets.REAL_DOMAIN }}
          PASSWORD=${{ secrets.PASSWORD }}
          HEADLESS=true
          SLOW_MO=0
          EOF
          echo "✅ 환경 설정 완료"
      
      - name: 🧪 테스트 실행
        id: test_step
        continue-on-error: true
        env:
          PYTHONPATH: ${{ github.workspace }}
          TEST_ENV: ${{ secrets.SERVER }}
          REAL_DOMAIN: ${{ secrets.REAL_DOMAIN }}
          PASSWORD: ${{ secrets.PASSWORD }}
        run: |
          # 이모지 제거하고 실제 테스트 매핑
          case "${{ inputs.test_target }}" in
            "🎯 전체 테스트")
              echo "🚀 전체 테스트 실행 중..."
              pytest tests/ -v --browser=${{ inputs.browser }}
              ;;
            "👤 구성원 (User)")
              echo "👤 구성원 테스트 실행 중..."
              pytest tests/test_user.py -v --browser=${{ inputs.browser }}
              ;;
            "📇 외부 연락처 (Contact)")
              echo "📇 외부 연락처 테스트 실행 중..."
              pytest tests/test_contact.py -v --browser=${{ inputs.browser }}
              ;;
            "👥 그룹 (Group)")
              echo "👥 그룹 테스트 실행 중..."
              pytest tests/test_group.py -v --browser=${{ inputs.browser }}
              ;;
            "🌐 외부 그룹 (External Group)")
              echo "🌐 외부 그룹 테스트 실행 중..."
              pytest tests/test_external_group.py -v --browser=${{ inputs.browser }}
              ;;
            "🏢 조직 (OrgUnit)")
              echo "🏢 조직 테스트 실행 중..."
              pytest tests/test_orgunit.py -v --browser=${{ inputs.browser }}
              ;;
            "💼 직책 (Position)")
              echo "💼 직책 테스트 실행 중..."
              pytest tests/test_position.py -v --browser=${{ inputs.browser }}
              ;;
            "📊 직급 (Level)")
              echo "📊 직급 테스트 실행 중..."
              pytest tests/test_level.py -v --browser=${{ inputs.browser }}
              ;;
            "📌 상태 (Status)")
              echo "📌 상태 테스트 실행 중..."
              pytest tests/test_status.py -v --browser=${{ inputs.browser }}
              ;;
            "👔 사용자 유형 (UserType)")
              echo "👔 사용자 유형 테스트 실행 중..."
              pytest tests/test_usertype.py -v --browser=${{ inputs.browser }}
              ;;
            "🏷️ 회사 태그 (Company Tag)")
              echo "🏷️ 회사 태그 테스트 실행 중..."
              pytest tests/test_company_tag.py -v --browser=${{ inputs.browser }}
              ;;
            "📍 MY 태그 (MY Tag)")
              echo "📍 MY 태그 테스트 실행 중..."
              pytest tests/test_my_tag.py -v --browser=${{ inputs.browser }}
              ;;
          esac
      
      - name: 📋 테스트 결과 요약 (성공)
        if: steps.test_step.outcome == 'success'
        run: |
          echo "### 🧪 테스트 실행 완료" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **선택한 테스트:** ${{ inputs.test_target }}" >> $GITHUB_STEP_SUMMARY
          echo "- **브라우저:** ${{ inputs.browser }}" >> $GITHUB_STEP_SUMMARY
          echo "- **실행 환경:** ${{ secrets.SERVER }}" >> $GITHUB_STEP_SUMMARY
          echo "- **실행 번호:** #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "#### ✅ 결과: 성공" >> $GITHUB_STEP_SUMMARY
          echo "모든 테스트가 정상적으로 통과했습니다." >> $GITHUB_STEP_SUMMARY
      
      - name: 📋 테스트 결과 요약 (실패)
        if: steps.test_step.outcome != 'success'
        run: |
          echo "### 🧪 테스트 실행 완료" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **선택한 테스트:** ${{ inputs.test_target }}" >> $GITHUB_STEP_SUMMARY
          echo "- **브라우저:** ${{ inputs.browser }}" >> $GITHUB_STEP_SUMMARY
          echo "- **실행 환경:** ${{ secrets.SERVER }}" >> $GITHUB_STEP_SUMMARY
          echo "- **실행 번호:** #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "#### ❌ 결과: 실패" >> $GITHUB_STEP_SUMMARY
          echo "위 로그에서 실패한 테스트를 확인하세요." >> $GITHUB_STEP_SUMMARY

